<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Categories de produits</a>
    <div class="dropdown-menu">
    <a href="{{ path("app_categories")}}"class="dropdown-item">--Toutes les categories--</a>

        {% for cat in categories_g.findAll() %}

            <a class="dropdown-item" href="{{ path("app_categorie_produits", {'id' : cat.id} ) }}">{{cat.nom}}</a>
            
        {% endfor %}
        
        
            
    </div>
</li>